#include<stdio.h>
int main()
{
    long long int x,y,z;
    while(scanf("%lld %lld",&x,&y)==2)
    {
        if(x,y)
        z=x-y;
        else
        z=y-x;
        printf("%lld",&z);
    }
    return 0;
}
